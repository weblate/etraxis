{% extends 'email.html.twig' %}

{% block message %}
    <p>
        {{ 'email.forgot_password.line1' | trans({ '%url%': url('homepage') }, 'passwords', locale) | raw }}
        {{ 'email.forgot_password.line2' | trans({}, 'passwords', locale) }}
    </p>
    <p>
        <a href="{{ url('reset_password', { 'token': token }) }}">{{ url('reset_password', { 'token': token }) }}</a>
    </p>
    <p>
        {{ 'email.forgot_password.line3' | trans({}, 'passwords', locale) }}
        {{ 'email.forgot_password.line4' | trans({}, 'passwords', locale) }}
    </p>
{% endblock %}
